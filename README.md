# מסע הקודים

משחק QR קצר בסגנון חדר בריחה אונליין: כל סריקה פותחת שלב, המשתתפים מקלידים קוד מספרי, תשובה נכונה מגלה חלק מהסיסמה הסופית, ובסוף מרכיבים את כל החלקים כדי לפתוח את דף הניצחון.

האתר בנוי כאתר סטטי ב-React + Vite, מתאים לפריסה פשוטה ב-Netlify, ומוכן להרחבה עתידית עם Backend או אזור ניהול.

## מה יש באתר

- 5 שלבי QR מוכנים: `/q/1` עד `/q/5`
- דף סופי: `/final`
- ממשק בעברית ו-RTL מלא
- תמיכה בשאלות מודפסות ליד ה-QR או שאלות שמופיעות באתר
- קלט מספרי לכל שלב
- אנימציית הצלחה עם חשיפת אות או חלק מהקוד
- אנימציית טעות וניסיון חוזר
- שמירת התקדמות בדפדפן
- כפתור מצב כהה
- קונפיגורציה מוכנה ל-Netlify

## איך המשחק עובד

1. מדפיסים לכל שלב QR שמוביל לכתובת ייחודית.
2. ליד כל QR אפשר להדפיס את השאלה או הרמז.
3. המשתתף נכנס לעמוד, מקליד את התשובה המספרית, ומקבל חלק מהקוד הסופי.
4. אחרי כל השלבים, נכנסים ל-`/final` ומקלידים את הקוד שנאסף.
5. אם הקוד נכון, מופיע מסך ניצחון.

## כתובות ל-QR

אחרי העלאה ל-Netlify, כל QR יכול לפתוח אחת מהכתובות האלו:

```text
/q/1
/q/2
/q/3
/q/4
/q/5
/final
```

לדוגמה, אם האתר עלה לכתובת:

```text
https://your-site-name.netlify.app
```

אז ה-QR הראשון צריך להוביל אל:

```text
https://your-site-name.netlify.app/q/1
```

## הרצה מקומית

צריך Node.js ו-npm.

```bash
npm install
npm run dev
```

אחרי ההרצה, האתר ייפתח מקומית בכתובת שמופיעה בטרמינל, בדרך כלל:

```text
http://127.0.0.1:5173/
```

## בדיקת Build

לפני העלאה ל-Netlify מומלץ להריץ:

```bash
npm run build
```

הפקודה יוצרת את תיקיית `dist`, שהיא תיקיית הפריסה של האתר.

## עריכת שאלות, תשובות וקודים

כל תוכן המשחק נמצא בקובץ:

```text
src/data/challenges.js
```

שם אפשר לשנות:

- `title` - שם השלב שמופיע באתר.
- `question` - השאלה שתופיע באתר. אם משאירים מחרוזת ריקה, האתר מציג הודעה שהשאלה נמצאת ליד ה-QR המודפס.
- `answer` - המספר הנכון שהמשתתף צריך להקליד.
- `reward` - האות או חלק הקוד שנחשף אחרי תשובה נכונה.
- `finalCode` - הקוד הסופי שצריך להקליד בדף `/final`.

דוגמה לשלב:

```js
{
  id: 1,
  path: "/q/1",
  title: "קוד 1",
  question: "כמה כוכבים מופיעים בציור?",
  answer: "7",
  reward: "א",
}
```

אם רוצים שהשאלה תהיה רק בדף המודפס, משאירים:

```js
question: ""
```

## הוספת שלב שישי

כדי להוסיף עוד QR, מוסיפים אובייקט נוסף למערך `challenges`:

```js
{
  id: 6,
  path: "/q/6",
  title: "קוד 6",
  question: "",
  answer: "123",
  reward: "!",
}
```

אחרי זה מעדכנים את `finalCode` כך שיכיל גם את החלק החדש.

## פריסה ב-Netlify

הפרויקט כבר כולל `netlify.toml` וקובץ redirects, כך שכתובות כמו `/q/1` ו-`/final` יעבדו גם אחרי רענון הדף.

הגדרות מומלצות ב-Netlify:

```text
Build command: npm run build
Publish directory: dist
```

אם מחברים את Netlify ל-GitHub, כל push ל-repo יכול להפעיל פריסה חדשה אוטומטית.

## אבטחה

כרגע האתר סטטי, ולכן מי שמבין בקוד יכול לבדוק את קבצי האתר ולמצוא את התשובות. למשחק חברים, אירוע משפחתי או פעילות קלילה זה בדרך כלל מספיק.

אם צריך להסתיר תשובות באמת, אפשר להוסיף בהמשך:

- Netlify Functions לבדיקת תשובות בצד שרת
- בסיס נתונים קטן לשאלות ותשובות
- אזור Admin ליצירת שלבים בלי לערוך קוד
- קודים חד-פעמיים או הגבלת ניסיונות

## רעיונות להמשך

- יצירת QR מתוך האתר עצמו
- עמוד Admin להוספת שלבים
- בחירת ערכת צבעים
- סאונד להצלחה וטעות
- טיימר קבוצתי
- מצב קבוצות עם ניקוד
- אנימציית סיום מותאמת אישית

## מבנה הפרויקט

```text
.
├── public/
│   └── _redirects
├── src/
│   ├── data/
│   │   └── challenges.js
│   ├── App.jsx
│   ├── main.jsx
│   └── styles.css
├── index.html
├── netlify.toml
├── package.json
└── vite.config.js
```

## טכנולוגיות

- React
- Vite
- Lucide React Icons
- Netlify-ready static deployment

## סטטוס

גרסה ראשונה מוכנה:

- 5 שלבים
- דף סיום
- עברית מלאה
- מצב כהה
- אנימציות בסיסיות
- התאמה למובייל
